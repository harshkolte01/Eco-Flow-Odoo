# UI Cleanup: Remove Reports Button

## Context
User requested to remove the "Reports" button from the header to clean up the UI.

## Changes
- Modified `frontend/components/Header.tsx`:
    - Removed the "Reports" button element.
    - Removed the `isReportsPage` variable.
